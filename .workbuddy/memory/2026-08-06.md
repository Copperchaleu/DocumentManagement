# 2026-08-06 工作日志

## 汇总文件页面精简（PeriodView.vue）
- 用户要求：删除左上角数量标签、删除 全选/筛选后全选/清空所选 三个按钮（行级勾选框已覆盖这些能力）。
- 改动：`web/src/views/PeriodView.vue` —— 删除 `.title-area`（含 `{{ filteredPeriodFiles.length }} 个` 计数标签）、删除三个 el-button、删除已无引用的 `selectAll`/`clearSelection` 两个 JS 函数。
- 保留：表格首列「选择」行勾选框 + 表头「筛选后全选」勾选框（依赖 `selectFilteredAll`/`toggleFilteredAll`，未删）、「复制路径（N）」按钮（复制 `row.abs_path`）。
- 验证：grep 确认删除项无残留；`npm run build` exit 0（412ms，无报错）。
- 注：本次 TeamCreate / Agent 工具报环境层错误（path / history），无法走标准 SOP 团队流转，主理人直接做精确删除 + 自跑构建复核替代 QA。

## 工作面板日历区域 UI 调整（TaskBoard.vue）
- 用户六项要求：①三外栏宽比不动（不碰 WorkbenchView）；②顶部 4 张统计卡恒定 4 列横排、窄屏等比例缩放不折叠；③待办列表 ↔ 日历互换（日历窄列 .82fr 在左、列表宽列 1.65fr 在右）；④日历标题缩到 14px/高26px 与切换按钮齐平、不换行；⑤日历底部改为「今日截止 N」+ 添加按钮，保留选中日任务列表、删「这一天还没有安排」；⑥移除整块提醒中心（模板+样式+upcomingReminders computed）。
- 改动：`web/src/components/TaskBoard.vue` —— 删除 `.workbench-side` 包裹层（calendar-card 直接挂 `.workbench-grid`）、`.workbench-grid` 列比例反转为 `.82fr 1.65fr`；`.metric-grid` 移除所有折叠断点恒为 `repeat(4, minmax(0,1fr))`；新增 `.calendar-card h2` 缩小字号规则；`.agenda-head` 替换为「今日截止 {{ todayCount }}」；删除 reminder-card 全部模板/样式及 `upcomingReminders`。保留 checkReminders/permissionState/enableNotifications/formatReminder 等被依赖逻辑。
- 走标准 SOP 团队流转（TeamCreate → 工程师 → QA），QA 跑 `npm run build` exit 0（1917 modules / 407ms），grep 确认 deleted 标识符无残留，IS_PASS YES。
- 决策点：按"这一天还没有安排"被单独点名删除，推断选中日任务列表应保留（仅删空状态文案）；日历互换时为保列表可读，将宽列随列表移动（日历窄列左移）。

## 汇总文件页：全选/计数/复制均基于筛选结果（PeriodView.vue）
- 用户要求：应用筛选后，点表头「选择」勾选框全选应只选筛选结果（而非筛选前全部）；复制路径按钮数量与复制内容也应基于筛选后数据集。
- 核查：全选逻辑 `selectFilteredAll`/`toggleFilteredAll` 早已遍历 `filteredPeriodFiles.value`（本身正确）；薄弱点在 `selectedCount`(原 `selectedIds.value.size`) 与 `selectedRows`(原 `appState.periodFiles.filter`)，在"切换筛选后隐藏已选行"等情况下会把筛选外的已选条目计入数量/复制。
- 改动：将 `selectedCount` 与 `selectedRows` 改为基于 `filteredPeriodFiles.value.filter(selectedIds.has)`，使数量统计与路径复制严格基于筛选后数据集；全选逻辑无需改。
- 验证：grep 确认无 `selectedIds.value.size` 与 `appState.periodFiles` 残留；`npm run build` exit 0（439ms）。
- 注：同前，TeamCreate/Agent 工具仍报错，主理人直接改 + 自跑构建复核。

## 工作面板·数据看板改造（DashboardPanel / TrendChart / 后端新接口）
- 用户三项要求：① 当日/当月/当季新增 3 张卡合并为 1 张（卡内三内联统计）；② 新增趋势三周期（日/月/季）全部改折线图（TrendChart 删柱状分支强制折线）；③ 新增「按分类统计」层级下钻：默认顶级分类，点有子分类者下钻到下一级直到叶子，每分类数量=该分类及全部子分类下 status!='draft' 的项目总数（已与用户确认口径）。
- 后端新增：`backend/database.py` `category_breakdown(parent_id)`（仅 1 次 `list_categories_flat()` + children_map 后序聚合子树总数，parent_id=None 返根级）；`backend/main.py` 模型 `CategoryBreakdownItem`/`CategoryBreakdownResponse` + `GET /api/stats/category-breakdown?parent_id=`（沿用 stats 纯 snake_case 无 alias 风格）。
- 前端：`web/src/api/index.js` 增 `getCategoryBreakdown(parentId)`；`DashboardPanel.vue` 概览卡合并 + 下钻状态(catParentId/catRows/catPath)与 drillInto/goUp/goRoot + 面包屑/返回导航 + 条形占比行（按当前层最大 project_total 归一化）；下钻并入 loadStats 以随外壳 refresh。
- 流程：走标准 SOP（TeamCreate→架构师→工程师→QA）。架构师首次 429 限流失败，重试成功；主理人因架构师延迟直接 synthesized 设计稿转交工程师，未阻塞。QA 用 TestClient 以临时库独立重算交叉验证 project_total 全一致、无 N+1、前端 build exit 0。IS_PASS YES。
- 清理：删 DashboardPanel 合并后遗留的死 CSS `.metric-rose .metric-icon`。
- 约定：按分类统计的"数量"语义 = 子树（含后代）saved 项目总数（非仅直接、非新增周期）。

## 数据看板：顶部概览卡等高 + 按分类改圆环图（DashboardPanel.vue）
- 用户两项要求：① 顶部「当日/当月/当季新增」概览卡与左侧待办区 4 张统计卡等高、横向对齐，三项保持单卡内横排；② 「按分类统计」由树形下钻改为圆环图（donut）。
- 解读：用户口述"按周、按月、按季度"，但后端 summary 仅 `today/month/quarter`（无 weekly 聚合），故保持 当日/当月/当季 语义不变，仅调布局与高度。已向用户说明此假设。
- 改动（仅 `web/src/components/DashboardPanel.vue` 单文件，未动后端/TaskBoard）：
  - 概览卡 `.metric-stat` 由「span/strong/small 直接堆叠」改为与待办卡同款「`.metric-stat-main`(标签|数值同行)+small」结构，使两栏卡片自然等高（min-height 均 98px、icon margin 均 8px）。
  - 删除整个树形下钻 UI（cat-nav 返回/顶级按钮、cat-breadcrumb、cat-row 条形行）及其 JS（catParentId/catPath/drillInto/goUp/goRoot/catMaxTotal/catBarWidth）；`loadStats` 改调 `loadCategoryBreakdown(null)`（仅顶级）。
  - 新增自绘 SVG 圆环图：viewBox 0 0 180 180、`rotate(-90 90 90)` 起笔 12 点、各分类 `circle` 以 `stroke-dasharray=len (circ-len)` + `stroke-dashoffset=-acc` 切扇区（CAT_COLORS 10 色调色板），中心显示 `catTotal`（项目总数），`catTotal===0` 时灰色整环；图例 cat-legend 列出 名称/数量/占比%。复用现有 `GET /api/stats/category-breakdown` 顶级数据（子树 project_total）。
- 验证陷阱（重要）：本轮工程师首轮只改了 `<script>` 漏改 `<template>/<style>`，模板仍引用已删变量——`npm run build` 不校验模板未定义变量，0 报错但运行时必崩（与先前 `computed is not defined` 同坑）。主理人独立 grep `catPath|goUp|goRoot|drillInto|catBarWidth|catMaxTotal` 零命中 + 重跑 build 0 报错 才放行。
- 约定：Vue SFC 改完后，QA 必须 grep 模板残留的已删标识符 + 跑 build，不能只靠 build 通过判断可用（build 抓不到模板运行时引用错误）。
- `npm run build` exit 0（419ms）。

## 数据看板概览卡回归修复（容器查询误伤）
- 用户截图反馈:概览卡三项仍竖排、不如待办卡等高——首版交付未达预期。
- 根因:`DashboardPanel.vue` 的 `<style scoped>` 保留了旧容器查询 `@container (max-width: 620px) { .metric-inline { grid-template-columns: 1fr; } }`。WorkbenchView 中间栏(数据看板)当前宽约 480px 且 `.workbench-col` 开了 `container-type: inline-size`,480<620 触发该断点,把三列压成单列;左侧待办卡无此断点故保持横排。
- 修复:删除该 `@container` 断点,`.metric-inline` 恒为 `repeat(3, minmax(0,1fr))`,三项横排、卡片高度与待办卡一致。保留 `@media (max-width: 860px) { .dashboard-metrics { grid-template-columns: 1fr } }`(不影响卡片内部)。
- 复用本次 BugFix 快捷路径(TeamCreate→工程师→主理人复核),grep `@container` 零命中 + build exit 0（448ms）放行。
- 教训:放在 `container-type: inline-size` 列内的卡片,其内部多列网格切勿为该列宽阈值(≤620px)设堆叠断点,否则三栏布局下必被误伤;多列横排需求应无条件保持。

## 数据看板：新增趋势加「周」+ 分类统计改多级 treemap（Request E）
- 用户两项要求：① 新增趋势增加一个按周(week)维度；② 分类统计由圆环图改为 ApexCharts 多维 treemap 样式（参考 apexcharts 官方 multi-dimensional treemap demo）。经 AskUserQuestion 确认：用 ApexCharts 组件 + 多级（顶级+直接子分类嵌套）。
- 主理人 6 项默认决策：weeks=12；series name 拼接顶级总数（如 `项目评审(20)`）；叶子顶级自身成独立矩形（不隐藏）；保留旧 `getCategoryBreakdown` 接口不动；treemap 调色板沿用靛蓝系为多色板（含少量 sky/cyan/emerald/amber/rose，前 4 基色为靛蓝 #4f46e5/#6366f1/#818cf8/#a5b4fc）；数据口径沿用现状（周趋势只数 saved；分类 project_total=子树 status!='draft' 总数）。
- 后端新增：`backend/path_utils.py` `list_recent_weeks(n=12)`（锚点本周一、ISO 周升序、label `YYYY-Www`）；`backend/database.py` `project_weekly_counts(weeks=12)`（按天查 saved 再 Python 归并 ISO 周、窗口补 0、WHERE status='saved'）+ `category_tree()`（list_categories_flat + children_map 后序记忆化子树聚合，返顶级+直接子分类 1 层，project_total=子树 !draft 总数，空库返 []）；`backend/main.py` 加 `elif range=="week": rows=db.project_weekly_counts(12)`（非法回退 day）、模型 `CategoryTreeChild`/`CategoryTreeItem`/`CategoryTreeResponse`（纯 snake_case 无 alias）、`GET /api/stats/category-tree`。
- 前端新增/改动：`web/package.json` 加 `apexcharts@^6.7.0` + `vue3-apexcharts@^1.11.1`（`npm install` 已装）；`web/src/main.js` 全局 `app.use(VueApexCharts)`（入口 main.js 非 main.ts）；`web/src/api/index.js` 加 `getCategoryTree`；`web/src/components/CategoryTreemap.vue`（新建，props items，每顶级一 series、data=直接子分类 {x,y} 或自身叶子、hasData 任一>0 才渲染、plotOptions.treemap distributed=false/enableShades=true/shadeIntensity=0.45、dataLabels 名称+数量、tooltip 显示 N 个项目）；`DashboardPanel.vue`（rangeOptions 加周、删 donut 全套改挂 `<CategoryTreemap :items="catTree">`、catTree/loadCategoryTree 接 loadStats）；`TrendChart.vue`（week 标签抽稀 step=ceil(12/6)=2，约 7/12 标签，day/month/quarter 各自保留）。
- 流程：标准 SOP（TeamCreate→架构师→工程师→双 QA 独立回归）。两 QA 均 IS_PASS YES：后端接口 HTTP 实测（真起 uvicorn 模块方式，因 main.py 用相对导入，须 `python -m uvicorn backend.main:app`，直接 import 文件会因相对导入失败）+ 独立 SQL 重算三处一致（周趋势 sum=20=saved 数、category_tree 各顶级/子 project_total 与递归 SQL 零偏差、非法 range 回退 day 14 日标签）；前端 grep 删 donut 标识符零命中、build exit 0、apexcharts 已打进 dist。
- 非阻塞提示：build chunk >1200kB 警告（apexcharts 增量）；treemap 调色板非纯靛蓝（前 4 靛蓝、余 6 杂色，产品经理若要求纯靛蓝可收口）；未做浏览器内 DOM 渲染（无浏览器环境），以构建+全局注册+契约静态核验+接口实测等价证明。
- 数据现状：当前库 28 项目=20 saved + 8 draft；category-tree 仅顶级「项目评审」total=20（下挂非 draft 子项），「会议纪要」「部门专项事务」total=0 → treemap 现基本空态「分类下暂无项目」，等用户正式保存更多项目即正常展开。此为空态正确行为，非 bug。
- 约定/坑：后端 main.py 用相对导入 `from .database import Database`，起服务必须用模块方式（`python -m uvicorn backend.main:app`）而不能 `import backend.main` 直接跑；QA 环境缺 httpx，用 urllib 真打 HTTP 或调 route 函数 + 独立 SQL 重算替代 TestClient。

## BugFix：数据看板 treemap 必须手刷才显示（keep-alive + ApexCharts 0 宽）
- 现象：分类 treemap 只有整页刷新才显示；切到其他选项卡再切回看板仍空白、须手刷。QA 未做浏览器内 DOM 渲染，故此前漏检。
- 根因两处：① `web/src/layouts/AppLayout.vue`(L208-212) 用 `<keep-alive>` 包 `<router-view>` → DashboardPanel 被缓存，`onMounted`(内调 loadStats)只在首次进看板跑一次，无 `onActivated` → 切回不重拉数据/不重绘；② `CategoryTreemap.vue` 的 `<apexchart v-if="hasData">` 在异步返回后才挂载，若那一刻容器宽 0（布局未稳）就以 0 宽渲染空白且不自愈，整页刷新因布局已稳才正常；切回时缓存态图表停在空白、series 未变 → 不重绘。
- 修复（仅改两个 .vue，未动后端）：`CategoryTreemap.vue` 加 `wrapRef`(根 div) + `chartRef`(apexchart) 两 ref、`refreshChart()`（优先 `chartRef.value.chart.resize()`，失败回退 `chartRef.value.refresh()`）、`onMounted` 内 `ResizeObserver` 监听 wrapRef（clientWidth>0 即重绘）+ `nextTick(refreshChart)`、`watch(items, nextTick(refreshChart), {deep:true})`、`onBeforeUnmount` 断开 RO；`DashboardPanel.vue` import 补 `onActivated`，加 `const catTreeKey = ref(0)` + `onActivated(() => { catTreeKey.value++; loadStats() })`，模板 `<CategoryTreemap v-else :items="catTree" :key="catTreeKey" />`。
- 验证：双 QA 独立回归 PASS（build exit 0；grep 确认两文件契约命中；读 `node_modules/vue3-apexcharts/dist/vue3-apexcharts-core.js` 确认 `chartRef.value.chart` 即 ApexCharts 实例、`resize()`/`refresh()` 均有效；确认 keep-alive 激活会向子树后代下发 `activated` 钩子 → onActivated 生效）。IS_PASS YES。
- 约定/坑（ApexCharts + Vue）：凡 `<apexchart v-if>` 异步挂载、或组件处 `<keep-alive>` 下，务必(a) 用 ResizeObserver 在容器有真实宽度时 `chart.resize()` 重绘，防 0 宽空白不自愈；(b) 切回时通过 `:key` 自增强制 treemap 重挂载。无浏览器环境无法做真实 DOM 渲染验证，等价证明用"构建通过 + 代码契约 + 第三方库实例 API + 框架钩子语义"。

## 分类级联选择器：选择任意一级 + 悬停展开漂移修复（cascader-drift）
- 排查结论：三处 el-cascader（项目编辑 ComposeView L358 / 项目列表筛选 ProjectsView L144 / 周期合并筛选 PeriodView L283）的 `categoryCascaderProps` 均已是 `checkStrictly: true` + `expandTrigger:'hover'` + `emitPath: true` → 即 Element Plus「选择任意一级选项」已实现且三页一致，无需改语义。
- 漂移根因：cascader 下拉 teleport 到 body 的 popper 默认带 `flip`；悬停展开更深层级面板变宽、逼近视口右边界时被 flip 从 `bottom-start`(左对齐)翻到 `bottom-end`(右对齐) → 整块下拉跳到左边。
- 修复（三页统一）：三处 `<el-cascader>` 均加 `popper-class="cat-cascader-popper"` + `placement="bottom-start"` + `:popper-options="{ modifiers: [{ name: 'flip', enabled: false }] }"`（禁用 flip 为核心修复）；并在 `web/src/styles/app.css` 末尾追加 `.cat-cascader-popper` 规则（`max-width: min(94vw,760px)`；面板 `flex-wrap:nowrap` + `overflow-x:auto`；菜单 `min-width:150px` + `border-right` 分隔线）。
- 教训：本次首轮工程师自报完成却漏加 app.css 全局规则（主理人独立 grep 零命中才抓出）；凡"改多文件 + 全局 CSS"的任务，主理人必须逐文件 grep + 读盘核验，不能只信成员 IS_PASS。
- 验证：双工程师实现 + 主理人独立读盘（三文件属性齐全、app.css 规则唯一无重复 L965-979）+ QA 独立回归 PASS（build exit 0；确认 `--border` 在 `:root` L36 已定义）；IS_PASS YES。

## BugFix：分类拖拽排序后新增分类顺序乱掉（sort_order 撞车）
- 现象：分类列表拖动排序后，新增一个分类会让之前拖好的顺序乱掉/看起来重置。
- 排查结论：reorder 本身没问题——`Database.reorder_siblings()` 事务内把兄弟 sort_order 写 0..n-1 并 commit 落库；前端 `CategoriesView.vue onNodeDrop` 也确实调 `reorderCategories`，frontend/dist 构建产物里接线也在。真凶在「新增」：`Database.create_category()` 新建默认 `sort_order=0`，与拖拽后第一个兄弟（也是 0）冲突，列表按 `(sort_order, name)` 排序时新项按名字插到中间甚至置顶 → 观感像重置。
- 修复（仅 `backend/database.py`）：`create_category` 形参 `sort_order: int = 0` → `sort_order: Any = _UNSET`（复用顶层哨兵 `_UNSET`）；事务内 INSERT 前若 `sort_order is _UNSET`，按 `parent_id` 取 `COALESCE(MAX(sort_order), -1)+1` 追加到末尾（parent_id None 用 `WHERE parent_id IS NULL`）。`main.py` 新增 handler(~L1230) 未传 sort_order → 自动走末尾追加，无需改。
- 验证：走 BugFix 快捷路径（TeamCreate→工程师→QA）。工程师回归 `qa/backend_reorder_test.py` 24 PASS（原 22 + 新增 2：reorder[c,b,a]后新增 sort_order=3/顺序[C,B,A,NewC]、删除后仍[C,B,A]）；QA 独立复现脚本 10/10（含重新实例化 Database 指向同 db 文件验证持久化、再增仍稳定追加）。全绿，源码/测试无 Bug。
- 无需重建前端（dist 已含 reorder 接线）；后端改完即生效，下次启动后端即带修复。

## 工作面板 4 项视觉打磨（UI-polish：标题横排/坐标轴字号/去空白）
- 用户四项要求（带 6 张截图）：①「新增趋势」(TREND) 与「按分类统计」(CATEGORIES) 区标题复用「TASKS 待办事项」样式，英文与中文左右排列（原 EN 在 CN 上方竖排）；② 趋势图坐标轴字体太小→加大；③ 趋势图下方空白删除；④ 待办事项列表下方空白删除。
- 改动（3 个 .vue，单文件纯 CSS，未动后端/JS 逻辑）：
  - `web/src/components/DashboardPanel.vue`：模板 TREND/CATEGORIES 两 header 加 `<div class="section-title">`(L130/L152) 包 kicker+<h2>；scoped 加 `.section-title { display:flex; align-items:baseline; gap:10px }` + `.section-title h2 { margin:0 }`(L190-191)；`.trend-body` 去 `min-height:250px` 仅留 `margin-top:14px`(L188)，`.trend-canvas` 清空(L189)。（`.trend-loading/.trend-empty` 的 `min-height:250px` 保留作空态/加载态，非本次空白 bug。）
  - `web/src/components/TrendChart.vue`：`.trend-axis-y`/`.trend-axis-x` `font-size` 11px→13px(L167-168)。
  - `web/src/components/TaskBoard.vue`：`.task-board { padding:21px }` 删 `min-height:540px`(L586)，`.task-list` 的 `max-height:418px; overflow:auto` 保留（滚动上限非空白 bug）；窄屏 `@media` 的 `.task-board{min-height:480px}`(L613) 与 `@container` 的 `min-height:auto`(L621) 保留为响应式回退。
- 验证：主理人独立读盘三文件 + `npm run build` exit 0；派 fresh `software-qa-engineer` 独立回归全部 PASS（A 标题横排/去空白、B 字号 13px、C 列表去空白、D 构建 0 报错；并确认无 `min-height:540px` 残留、空态/响应式保留项不误报）。IS_PASS YES。
- 走快速模式变体（TeamCreate→工程师→QA 独立回归），双核验一致通过。

## 分类新增位序微调：从「末尾追加」改为「插入最前」
- 用户要求：上一轮修复「拖拽排序后新增顺序乱」后，新建分类默认追加到同级末尾（MAX+1），现要求改为插到同级最前。
- 改动（仅 `backend/database.py`）：`create_category` 的 `sort_order is _UNSET` 分支由「查 MAX+1 追加末尾」改为「sort_order=0 置顶 + 同级其余兄弟 `UPDATE categories SET sort_order=sort_order+1` 整体后移」（parent_id None 用 `WHERE parent_id IS NULL`，否则 `WHERE parent_id=?`）。显式传值分支不变；main.py 新增 handler 不传 sort_order → 自动走最前插入。
- 测试更新：`qa/backend_reorder_test.py` 两个用例断言改为 `sort_order==0`、顶层顺序 `[NewC,C,B,A]`（删除后仍 `[C,B,A]`）。
- 验证：BugFix 快捷路径（TeamCreate→工程师→QA）。工程师回归 `qa/backend_reorder_test.py` 24 PASS；QA 独立复现脚本 `/tmp/qa_verify_insert_front.py` 14/14（含重新实例化 Database 指向同 db 文件验证持久化、reorder→新增置顶、删除不影响其余、连续新增仍置顶）。全绿，源码/测试无 Bug。
- 注：之前已拖拽排好的顺序不会被破坏——新项插最前，其余后移一位；后端改完即生效，无需重建前端 dist。
