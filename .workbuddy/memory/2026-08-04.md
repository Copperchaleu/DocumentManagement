# 2026-08-04 工作日志

## 项目初始化并提交到 GitHub
- 原目录不是 git 仓库，已 `git init -b main`，添加远程 `https://github.com/Copperchaleu/DocumentManagement.git`
- 仓库级身份：`user.name=sylvi`、`user.email=sylvi@local`（未用 --global，避免影响其他仓库）
- 调整 `.gitignore`：把 `frontend/dist/` 整目录忽略（原仅忽略 `frontend/dist/.vite/`，导致大量历史构建产物被暂存）；`data/`、`*.db`、`*.docx`、`web/node_modules/` 已正确忽略
- 首次提交 3511cc9，共 150 个文件（源码 + README + 配置，不含构建产物/数据库/Word 数据）
- 已 `git push -u origin main`，远程新建 `main` 分支并设跟踪
- 提醒：部署或 clone 后需 `npm run build`（前端）、`pip install -r requirements.txt`（后端）才能运行；`.gitignore` 已忽略产物与数据，数据库需单独备份
